<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MarketBuoy{% block title %}{% endblock %}</title>
    <!-- Bootstrap --> 
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
</head>
<body>
<!-- NAVBAR -->
<!-- TODO: FIX COLLAPSE FOR SMALLER VIEW --> 
<nav class="navbar navbar-expand-sm navbar-dark bg-dark shadow mb-3">
    <div class="container-fluid">
        <div>
            <a class="navbar-brand fs-2" href="/">MarketBuoy</a>
        </div>
        <div class="collapse  navbar-collapse d-flex justify-content-end">
            <ul class="navbar-nav fs-4">
                {% if current_user.is_authenticated %}
                <li class="nav-item"><a class="nav-link text-light" href="/u/{{ current_user.username }}">Home</a></li>
                <li class="nav-item"><a class="nav-link text-light" href="/collections">Collections</a></li>
                <li class="nav-item"><a class="nav-link text-light" href="/auth/logout">Logout</a></li>
                {% else %}
                <li class="nav-item"><a class="nav-link text-light" href="/collections">Collections</a></li>
                <li class="nav-item"><a class="nav-link text-light" href="/auth/login">Login</a></li>
                <li class="nav-item"><a class="nav-link text-light" href="/auth/signup">Sign-up</a></li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>

<!-- FLASH MESSAGES --> 
<div class="container">
    {% with messages = get_flashed_messages(with_categories=true) %}
    {% if messages %}
        {% for category, message in messages %}
            {% if category == "error"%}
                <div class="alert alert-danger text-center">{{ message }}</div>
            {% else %}
                <div class="alert alert-primary text-center">{{ message }}}</div>
            {% endif %}
        {% endfor %}
    {% endif %}
    {% endwith %}
</div>

<!-- CONTENT -->
<div id="content" class="container">
    {% block content %}{% endblock %}
</div>
<!-- Charts.js & Related Scripts -->
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script src="/static/chart-scripts.js"></script>
</body>
</html>